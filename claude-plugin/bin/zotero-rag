#!/usr/bin/env bash
# Zotero RAG — semantic search for Zotero collections
# Usage: zotero-rag search --collection "数字经济" "query"
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON_DIR="$(cd "$SCRIPT_DIR/../../python" && pwd)"
cd "$PYTHON_DIR" && exec uv run zotero-rag "$@"
