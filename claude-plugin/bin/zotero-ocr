#!/usr/bin/env bash
# Zotero OCR — cloud OCR for Zotero collections
# Usage: zotero-ocr --collection "数字经济" | zotero-ocr --item 12345
set -euo pipefail
exec uv run --project "${CLAUDE_PLUGIN_ROOT}/python" zotero-ocr "$@"
