# XPI External API Audit — 2026-04-23

Zotero 8.0.4 reality-check of every `Zotero.*` call across the XPI's
9 handler files. Row format and verdict set defined in
`2026-04-23-xpi-api-audit-design.md`.

---

---


Target: `src/handlers/items.ts` (Zotero 8.0.4 runtime).
Row format: `| handler:line | zotero-call | doc-ref | verdict | note |`.
Verdict set: `✓correct` / `✗signature` / `⚠deprecated` / `⚠cumbersome` / `?unverifiable`.

**Scope note on row count:** the audit's narrow enumeration regex `Zotero\.\w+\.\w+|new Zotero\.\w+` matches 32 calls in the source, but the `findDuplicates` flow drives 3 additional Zotero-semantic calls that the regex misses — (1) `new (Zotero as any).Duplicates(libraryID)` at line 255, which the regex rejects because of the `as any` typecast wrapper; (2) `duplicates.getSearchObject()` at line 256, a call on a Zotero-returned *instance*; (3) `duplicates.getSetItemsByItemID()` at line 257, also an instance call. All three carry findings worth cataloging for Phase 4 (in particular the ✗signature on line 257), so the table below contains **35 rows** (32 grep-matched + 3 instance-method / typecast extras). Phase 6's concatenation uses a broader match that covers all three.

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| items.ts:23 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar ID returns a single item or `false`. Handler null-checks with `if (!item)` which matches the `false` return for missing. |
| items.ts:35 | `new Zotero.Item(params.itemType)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L30 | ✓correct | Constructor `function (itemTypeOrID)` accepts itemType string. `as any` cast is only there to quiet TS. |
| items.ts:36 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Static read-only getter via `Zotero.defineProperty`. Assigning to `item.libraryID` before save is the standard pattern. |
| items.ts:76 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Same scalar form, same null guard. |
| items.ts:115 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Same scalar form, same null guard. |
| items.ts:122 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Same scalar form, same null guard. |
| items.ts:130 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Same scalar form, same null guard. |
| items.ts:140 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Static property read. |
| items.ts:141 | `Zotero.Items.getAll(libraryID, false, true)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/items.js#L121 | ⚠cumbersome | Args match (`getAll(libraryID, onlyTopLevel, includeDeleted, asIDs)`, `includeDeleted=true` for trash), but handler then loads EVERY item in library and filters by `deleted && !isNote && !isAttachment` in JS. Zotero ships a dedicated trash helper — `Zotero.Items.getDeleted(libraryID, asIDs, days)` (items.js#L1085) returns only trashed IDs in one call. |
| items.ts:153 | `Zotero.Items.getAsync(params.ids)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form returns array of items. |
| items.ts:157 | `Zotero.DB.executeTransaction(async () => { ... })` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/db.js#L432 | ✓correct | Standard batch-transaction wrapper `executeTransaction(func, options)`. Each `item.save()` inside is the non-Tx variant, which is the documented pattern. |
| items.ts:168 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Static property read. |
| items.ts:169 | `Zotero.Items.getAll(libraryID, false, false)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/items.js#L121 | ⚠cumbersome | Loads every item in library to grab 20 recent. `getAll` has no ORDER/LIMIT. For "recent by dateAdded/dateModified", the idiomatic approach is a `Zotero.Search` with sort conditions or a direct `Zotero.DB.queryAsync` for the top-N IDs. Works, but O(library size) for a `limit=20` query. |
| items.ts:180 | `new Zotero.Translate.Search()` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2694 | ✓correct | No-arg constructor for identifier-based (DOI/ISBN/PMID/arXiv) search translators. |
| items.ts:184 | `translate.translate({ libraryID: Zotero.Libraries.userLibraryID })` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2188 (Web.prototype.translate — Search inherits Base.prototype.translate, same options shape) | ⚠cumbersome | Works — resolves to `newItems[]`. But when `params.collection` is passed the handler skips it here and then loops `item.addToCollection + saveTx` in a second pass (lines 188-193). `translate()` already accepts a `collections: [id]` option that does this atomically inside the same transaction. One call saves two round-trips per item. |
| items.ts:198 | `new Zotero.Translate.Web()` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2084 | ✓correct | No-arg constructor for DOM-based (URL/page) translators. |
| items.ts:200 | `Zotero.HTTP.processDocuments(params.url, (doc: any) => doc)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/http.js#L1374 | ✗signature | `processDocuments(urls, processor, options)` normalizes `urls` to an array (`if (typeof urls == "string") urls = [urls]`) and always returns `Promise<Array>` (array of processor results, even for a single URL). Handler assigns the array to `doc` and passes to `setDocument(doc)` which expects a single Document — detection silently fails. Correct form: `const [doc] = await Zotero.HTTP.processDocuments(url, d => d)`. |
| items.ts:207 | `translate.translate({ libraryID: Zotero.Libraries.userLibraryID })` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2188 | ⚠cumbersome | Same pattern as line 184 — `collections` option would fold the second-pass loop (lines 208-213) into the initial translate. |
| items.ts:218 | `new Zotero.Translate.Search()` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2694 | ✓correct | Identifier search for ISBN. |
| items.ts:222 | `translate.translate({ libraryID: Zotero.Libraries.userLibraryID })` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2188 | ⚠cumbersome | Same as lines 184/207 — `collections` option collapses the second-pass save loop. |
| items.ts:236 | `Zotero.File.pathToFile(params.path)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/file.js#L43 | ✓correct | `pathToFile(pathOrFile)` accepts string path, returns `nsIFile`. `.exists()` check on the result is standard. |
| items.ts:240 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Static property read. |
| items.ts:242 | `Zotero.Attachments.importFromFile({file, libraryID, collections})` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/attachments.js#L58 | ✗signature | `importFromFile(options)` returns a single `Zotero.Item`, never an array. Handler's `Array.isArray(importedItems)` branch at line 247 is dead code and misleading. Correct form: drop the array branch, just `return [serializeItem(importedItems)]`. Secondary: `parentItemID` and `collections` are mutually exclusive per the source — passing `[]` when no collection is intentional works but is cleaner as `undefined`. |
| items.ts:254 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Static property read used to scope the Duplicates search. |
| items.ts:255 | `new Zotero.Duplicates(libraryID)` (via `(Zotero as any).Duplicates` typecast) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/duplicates.js#L26 | ✓correct | Constructor `Zotero.Duplicates = function (libraryID)` requires libraryID (throws if undefined). `as any` only because the bundled type defs omit `Duplicates`. |
| items.ts:256 | `duplicates.getSearchObject()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/duplicates.js#L48 | ⚠cumbersome | Works but the handler never uses the returned `Zotero.Search` — it's called purely for side effect (populating `_sets`). Fragile: `getSearchObject()` creates a temp table (`tmpDuplicates_<rand>`) and registers an unload listener (duplicates.js#L54) that expects `CollectionTreeRow` to drop it; abandoning the search leaks the temp table until the session's DB handle closes. Idiomatic enumeration: keep the returned `search`, call `await search.search()` to get member IDs, then group via `getSetItemsByItemID(id)`. |
| items.ts:257 | `duplicates.getSetItemsByItemID()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/duplicates.js#L89 | ✗signature | Zotero 8 signature is `getSetItemsByItemID(itemID)` — takes ONE itemID and returns a flat array of sibling IDs. Handler calls with ZERO args and iterates the return via `Object.entries(ids)` as if it were a map keyed by itemID. The enumeration is non-functional — `_getObjectFromID(undefined)` will return a falsy result and `findAllInSet` will return an empty array, yielding `{groups: [], totalGroups: 0}` unconditionally. Correct form: `const search = await duplicates.getSearchObject(); const memberIDs = await search.search(); const seen = new Set(); const groups = []; for (const id of memberIDs) { if (seen.has(id)) continue; const set = duplicates.getSetItemsByItemID(id); for (const sid of set) seen.add(sid); groups.push(set); }`. |
| items.ts:274 | `Zotero.Items.getAsync(params.ids)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form returns array of items. |
| items.ts:277 | `Zotero.Items.merge(master, [items[i]])` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/items.js#L975 | ✓correct | `merge(item, otherItems)` — first arg = master Item, second = array of other Items. Handler merges one-at-a-time in a loop; merging the whole array in a single call (`Zotero.Items.merge(master, items.slice(1))`) would be cheaper but the loop is semantically equivalent. |
| items.ts:283 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar fetch + null guard. |
| items.ts:288 | `Zotero.Items.getByLibraryAndKeyAsync(item.libraryID, key)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L338 | ✓correct | `getByLibraryAndKeyAsync(libraryID, key, options)`; `item.relatedItems` returns an array of string keys (item.js#L6099 `_getRelatedItems` pushes keys via `Zotero.URI.getURIItemLibraryKey`). Resolving each key through this call is exactly the documented pattern. |
| items.ts:295 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar fetch. |
| items.ts:296 | `Zotero.Items.getAsync(params.relatedId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar fetch. |
| items.ts:306 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar fetch. |
| items.ts:307 | `Zotero.Items.getAsync(params.relatedId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar fetch. `addRelatedItem` (item.js#L1304) and `removeRelatedItem` (item.js#L1329) are unidirectional — each only calls `addRelation`/`removeRelation` on `this` — so the handler correctly calls both sides + saves both. |

---


Target: `src/handlers/collections.ts` and `src/handlers/export.ts` (Zotero 8.0.4).
Row format and verdict set: see `2026-04-23-xpi-api-audit-design.md`.

Scope note: this table may include calls on Zotero-returned instances that the
narrow `Zotero\.\w+\.\w+|new Zotero\.\w+` regex does not pick up (e.g.
`col.save()`, `translate.setItems()`). When such a call carries a finding, add
it as its own row keyed on its real line number — the row total will exceed the
bare grep count. Declare the actual total in this note after Step 3.

**Actual enumeration totals declared here after Step 3:**

- `collections.ts`: narrow-grep finds **16** `Zotero.*` call sites. No extra
  instance-method findings were severe enough to warrant split rows — every
  `col.saveTx()`, `col.eraseTx()`, `col.getChildItems()`, `col.getChildCollections()`,
  `item.addToCollection()`, `item.removeFromCollection()`, `item.saveTx()` call
  uses the documented signature correctly. Table therefore has **16 rows**. The
  task plan said "expected 17" — that was a plan-side miscount; the file truly
  has 16 matches (see Step 1 output in commit message).
- `export.ts`: narrow-grep finds **4** `Zotero.*` call sites (one of which is
  the comment on L48 referring to `Zotero.QuickCopy.getContentFromItems`, which
  is no longer called — retained as an audit row explaining the bypass). The
  `bibliography` handler additionally performs several Zotero-instance calls
  (`Zotero.Styles.get`, `style.getCiteProc`, `engine.setOutputFormat`,
  `engine.updateItems`, `engine.makeBibliography`, `engine.free`) that the
  narrow regex misses — these are the load-bearing API surface of the 2026-04-23
  bibliography rewrite and MUST be audited. Each gets its own row. Similar
  treatment for the `exportItems` helper's instance methods
  (`translate.setItems`, `translate.setTranslator`, `translate.setHandler`,
  `translate.translate`, `translate.string`) and the `citationKey` handler's
  `item.getField` / `item.getCreators`. Table therefore has **18 rows** total
  (4 grep-matched + 14 instance-method / property extras). The task plan said
  "expected 5" — again a plan-side miscount plus the deliberate scope widening
  for the bibliography rewrite's API surface.

Total rows across both sections: **34**. Phase 6's concatenation uses a broader
match that covers all of them.

## collections.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| collections.ts:24 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L30 | ✓correct | Static read-only getter via `Zotero.defineProperty(this, 'userLibraryID', {...})`. Returns the integer libraryID of the user's personal library. |
| collections.ts:25 | `Zotero.Collections.getByLibrary(libraryID, false)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/collections.js#L71 | ✓correct | Signature is `getByLibrary(libraryID, recursive=false, includeTrashed=false)`. `recursive=false` returns only top-level (rootless) collections. For the RPC `list` method — which the CLI uses to render a flat list for pickers — this is the documented semantic. Arguably surprising (a caller might expect "all collections in library"), but matches the method's documented contract. |
| collections.ts:30 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Inherited from `Zotero.DataObjects.prototype.getAsync`. Scalar integer ID returns a single Collection or `false`; handler's `if (!col)` null-guard matches. |
| collections.ts:36 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Same scalar form + null guard. |
| collections.ts:49 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Same scalar form + null guard. `col.getChildCollections(false)` (collection.js#L200) then returns non-trashed child Collections as instances (asIDs=false). |
| collections.ts:56 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L30 | ✓correct | Static property read. |
| collections.ts:57 | `Zotero.Collections.getByLibrary(libraryID, false)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/collections.js#L71 | ✗signature | This is the `tree` handler. `recursive=false` (collections.js#L114) returns ONLY top-level collections — subcollections are never fetched. `buildTree` at lines 5-20 then iterates this flat root-only list and builds a map keyed by id; because no root's `parentID` appears in the map (roots by definition have no parent in this set), every collection becomes its own root node with `children: []`. The subtree data is never materialized. Correct call: `Zotero.Collections.getByLibrary(libraryID, true)` — `recursive=true` returns the full hierarchy (collections.js#L116-130 iterates `getDescendents`), which `buildTree` can then fold into a proper nested structure via `parentID` lookups. Without this fix, `collections.tree` returns a degenerate flat tree. |
| collections.ts:62 | `new Zotero.Collection()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/collection.js#L25 | ✓correct | Constructor accepts optional `params` object (collection.js#L34: `assignProps(this, params, ['name', 'libraryID', 'parentID', 'parentKey'])`). Calling with no args is the documented "empty, then assign fields" pattern — confirmed in Zotero's own test suite (test/tests/collectionTest.js builds collections this way). Handler immediately sets `libraryID`, `name`, optional `parentID` on the returned instance. |
| collections.ts:63 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L30 | ✓correct | Assigned to `col.libraryID`; the `libraryID` setter (collection.js#L56) delegates to `this._set('libraryID', val)`, the standard write path. `as any` typecast is only to quiet TS — the property is definitely writable on a freshly-constructed, unsaved collection. |
| collections.ts:71 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Same scalar form + null guard. Mutation path: `col.name = params.name; await col.saveTx();` — `saveTx` (dataObject.js#L1012) wraps `save()` in a transaction. Standard mutate-then-save pattern. |
| collections.ts:79 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Same scalar form. `col.eraseTx()` (dataObject.js#L1316) is the transactional erase — standard path. |
| collections.ts:86 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Same scalar form. Move path: `(col as any).parentID = params.newParentId OR_ELSE false; await col.saveTx()` (OR_ELSE stands in for the JS short-circuit-or operator — markdown tables mangle the literal pipe). Setting `parentID = false` is the documented way to unset the parent (dataObject.js#L270-281 `_setParentID`: `id ? ... : false`; dataObject.js#L298-310 `_setParentKey` accepts `false`). |
| collections.ts:94 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | addItems path: scalar fetch + null guard. |
| collections.ts:97 | `Zotero.Items.getAsync(itemId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ⚠cumbersome | Loop-fetching items one at a time inside the handler (N round-trips instead of 1) when `Zotero.Items.getAsync(params.itemIds)` with the array form (dataObjects.js#L261; see items.ts audit rows for items.ts:153/274 for the verified array-form usage) would fetch all in a single call. Then the handler calls `item.addToCollection(params.id); await item.saveTx()` per item — this could be wrapped in `Zotero.DB.executeTransaction(async () => { for (const item of items) { item.addToCollection(params.id); await item.save(); } })` for a single transaction (see items.ts:157 for the verified pattern). Functionally correct; just O(N) transactions instead of 1. Alternative: `Zotero.Collection.prototype.addItems(itemIDs, options)` (collection.js#L407) does exactly this in a single call — takes an array of itemIDs and does the transactional add — handler should prefer `col.addItems(params.itemIDs)`. |
| collections.ts:108 | `Zotero.Items.getAsync(itemId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ⚠cumbersome | Symmetric to line 97. `Zotero.Collection.prototype.removeItems(itemIDs)` (collection.js#L452) is the batch equivalent. Additionally the handler lacks any "does the item exist?" — it silently skips missing items, which is defensible, but combined with the N+1 round-trips this is noticeably slower than the one-shot `col.removeItems(params.itemIds)`. |
| collections.ts:118 | `Zotero.Collections.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | stats handler: scalar fetch + null guard. Uses `col.getChildItems(false)` (collection.js#L225 — `asIDs=false, includeTrashed=undefined`: returns non-trashed items as Item instances) and `col.getChildCollections(false)` (collection.js#L200 — same semantics for collections). Then filters with `i.isNote()` (item.js#L2257) and `i.isAttachment()` (item.js#L2460) to break down counts — all standard. |

## export.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| export.ts:5 | `Zotero.Items.getAsync(ids)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Array form — `getAsync(array)` returns an array of Item instances (documented on `singleObject` branch at dataObjects.js#L274). The exporter consumes this directly via `translate.setItems(items)`. |
| export.ts:6 | `new Zotero.Translate.Export()` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2498 | ✓correct | Constructor: `Zotero.Translate.Export = function() { this.init(); }` — no args. Export inherits from `Zotero.Translate.Base()`. |
| export.ts:7 | `translate.setItems(items)` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2510 | ✓correct | `Export.prototype.setItems = function(items) { this._export = {type: 'items', items: items}; }` — accepts an array of `Zotero.Item` instances. Matches exactly. |
| export.ts:8 | `translate.setTranslator(translatorID)` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2536 | ✓correct | Export-specific override forwards to Base at translate.js#L920. Accepts a translator UUID string — `setTranslator("9cb70025-...")` stores as `[translatorID]` on `this.translator` (Base signature documented to accept string or translator-object). The four TRANSLATORS constants (bibtex/ris/cslJson/csv) are all canonical Zotero core translator UUIDs. |
| export.ts:10 | `translate.setHandler("done", (_obj, status: boolean) => ...)` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L995 | ✓correct | `setHandler(type, handler)` registers a callback. The "done" handler is invoked via `_runHandler("done", status)` at translate.js#L1569/#L1699/#L1703/#L1706 with `(translate, status)` — `status` is `true` on success, `false` on failure. Handler's `if (status) resolve(...) else reject(...)` matches that contract. |
| export.ts:11 | `translate.string` (property read) | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2559 | ✓correct | `Export.prototype.complete` sets `this.string = this._io.string` when the IO backend is `Zotero.Translate.IO.String`. For the string-producing translators bibtex/ris/cslJson/csv — which don't write files — the Export path goes through `IO.String` and the property is populated before "done" fires. Matches the resolve pattern. |
| export.ts:14 | `translate.translate()` | https://github.com/zotero/translate/blob/master/src/translation/translate.js#L2670 | ✓correct | `Export.prototype.translate = function() { ... return Zotero.Translate.Base.prototype.translate.apply(this, arguments); }` delegates to Base (translate.js#L1246). No args needed for Export — setItems + setTranslator is sufficient setup. |
| export.ts:48 | `Zotero.QuickCopy.getContentFromItems` (comment-only reference) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/quickCopy.js | ✓correct | This is a comment explaining why we bypass QuickCopy; there is no runtime call. Retained as an audit row because the narrow regex matches it. The bypass is correct per 2026-04-23 commit f03f62d — Zotero 8's QuickCopy internal path called `style.getCiteProc` on the wrong object, surfacing as `-32603`. Going through `Zotero.Styles` directly (next rows) is the documented path. |
| export.ts:54 | `Zotero.Styles.get(styleId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/style.js#L234 | ✓correct | `this.get = function (id, skipMappings)` — returns the `Zotero.Style` object from `_styles[id]` cache, or `false` if not installed. Handler's `if (!style)` branch matches the `false` return. The `(Zotero as any)` cast is only because TS defs omit `Styles`. |
| export.ts:65 | `style.getCiteProc()` (instance call on Zotero.Style) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/style.js#L731 | ⚠cumbersome | `getCiteProc(locale, format, options = {})` accepts `format` as a second arg. Handler calls `style.getCiteProc()` (no args) then calls `engine.setOutputFormat(fmt)` inside the loop. Could pass `format` directly: `style.getCiteProc(undefined, fmt)`. Functionally identical because `setOutputFormat` switches format cheaply (style.js#L745 comment explicitly documents this as intentional). Minor idiom. More importantly: calling `getCiteProc()` twice inside a loop (once per html/text format) builds a fresh CiteProc engine each time — each build loads XML and constructs a new `Zotero.CiteProc.CSL.Engine` (style.js#L837). The `cache: true` option (style.js#L746) would cache across calls, but Handler doesn't pass it. For a 2-iteration loop the cost is bounded but wasteful. Idiomatic: build once, switch output format, re-run. |
| export.ts:67 | `engine.setOutputFormat(fmt)` (citeproc-js API) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/style.js#L754 | ✓correct | Standard citeproc-js method — Zotero itself calls `citeproc.setOutputFormat(format)` at style.js#L754 (cached path) and #L846 (fresh-engine path). `fmt` is `"html"` or `"text"`, both valid citeproc output formats. |
| export.ts:68 | `engine.updateItems(itemIDs)` (citeproc-js API) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/cite.js#L195 | ✓correct | Standard citeproc-js bulk-register method — Zotero's own `Zotero.Cite` helpers call `cslEngine.updateItems(items.map(item => item.id))` (cite.js#L195). Passing raw numeric itemIDs is the documented form — citeproc-js then calls back into Zotero's sys layer (`retrieveItem`) to load each item's CSL-JSON. |
| export.ts:69 | `engine.makeBibliography()` (citeproc-js API) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/cite.js#L255 | ✓correct | Standard citeproc-js method — returns `[metadata, entriesArray]` on success or `false` on failure. Handler correctly guards `if (bib && Array.isArray(bib) && bib.length >= 2)` before indexing `bib[1]`. Matches Zotero's own usage at cite.js#L255. |
| export.ts:75 | `engine.free?.()` (citeproc-js API via Zotero Cite) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/style.js#L847 | ✓correct | `free` is a method Zotero attaches to CSL engines explicitly: style.js#L847 `citeproc.free = () => 0;` for the CSL path. For the CiteprocRs path (style.js#L822-834) free is not attached, hence the handler's optional-chaining `engine.free?.()` — guards against both absence and the CiteprocRs branch lacking free semantics. Robust. |
| export.ts:88 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L261 | ✓correct | Scalar fetch + null guard. Standard form. |
| export.ts:91 | `item.getField("extra")` (instance call on Zotero.Item) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L561 | ✓correct | `getField(field, unformatted, includeBaseMapped)` — passing `"extra"` returns the free-form Extra field as a string. Standard read. |
| export.ts:93 | `item.getField("year")` (instance call on Zotero.Item) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L561 | ⚠cumbersome | `"year"` is not a primary Zotero field. Items store `date` (full ISO-ish date) via `getField("date")`. `getField("year")` either returns empty (if no base-mapping resolves "year" to a real field) or — with `includeBaseMapped` defaulted to false — returns undefined for item types whose date field is aliased to "date". For reliable year extraction, use `Zotero.Date.strToDate(item.getField("date")).year`. As-is, the citation-key fallback for items without a Better BibTeX key will often emit `<Lastname>` with no year suffix. Not a crash — a silent undercharacterization. |
| export.ts:93 | `item.getCreators()` (instance call on Zotero.Item) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L1131 | ✓correct | Returns an array of plain-object creator data `{firstName, lastName, fieldMode, creatorTypeID}` (item.js#L1131-1141). Handler's `[0]?.lastName` is safe on empty arrays. |

---


Target: three handlers, Zotero 8.0.4. Row format / verdicts per design doc
(`2026-04-23-xpi-api-audit-design.md`).

Scope note: this table may include calls on Zotero-returned instances that the
narrow `Zotero\.\w+\.\w+|new Zotero\.\w+` regex does not pick up. Such calls
get their own rows keyed on real line numbers when they carry findings, so row
totals may exceed narrow-grep counts.

**Actual enumeration totals declared here after Step 3:**

- `attachments.ts`: narrow-grep finds **15** `Zotero.*` call sites (the task
  plan said "expected 16" — plan-side miscount; confirmed `grep -cE ... =
  15`). All 15 are audited. Instance-method calls
  (`parent.getAttachments`, `att.getField`, `att.getFilePath`,
  `att.getFilePathAsync`, `item.isAttachment`, `file.exists`) are correct
  against item.js and file.js, no split rows needed. Table has **15 rows**.
- `notes.ts`: narrow-grep finds **14** `Zotero.*` call sites (matches plan).
  Two extra rows split out: `s.addCondition("note", "contains", query)` on
  line 48 (load-bearing — verifies the `note` condition name + operator), and
  `pdfItem.getAnnotations()` on line 71 (the cast `as unknown as number[]` is
  a lie — see finding). Table has **16 rows**.
- `search.ts`: narrow-grep finds **19** `Zotero.*` call sites (plan said "20";
  plan-side miscount). Table has **23 rows** — four extra rows split out for
  instance-method calls that the narrow regex misses but whose correctness is
  load-bearing for every search method: `s.addCondition(...)` (search.ts:11,
  12) and `s.search()` (search.ts:13) are all audited against search.js#L297
  / search.js#L541; the `addCondition("noChildren", ...)` pattern gets one
  shared row at search.ts:12 — the same idiom appears 5 times (L12/L29/L44/
  L60/L77) and the fix is a single `replace_all`. Note: `search.byIdentifier`
  still types `params.pmid?: string` in its signature (line 70) even though
  commit `b7b5e21` removed `--pmid` from the CLI — no handler branch uses it.
  Already catalogued in prior tasks; not re-flagged as its own row.

Total rows across three sections: **54** (15 + 16 + 23). Grep total is **48**
(15 + 14 + 19). The +6 widening is from instance-method splits (2 in notes.ts,
4 in search.ts) that carry findings or verify load-bearing signatures.

## attachments.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| attachments.ts:7 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar integer → single Item or `false`; null-guard matches contract (dataObjects.js#L167-170 wraps scalar into single-element array, returns first element; falsy return for missing id at #L197-201). |
| attachments.ts:10 | `Zotero.Items.getAsync(attachmentIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form → array of Items. `attachmentIDs` comes from `parent.getAttachments()` (item.js#L3969) which returns integer IDs, so this call is typed correctly. |
| attachments.ts:22 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. Standard `getFulltext` entry point. |
| attachments.ts:28 | `Zotero.Items.getAsync(attIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. `attIDs` from `item.getAttachments()` (item.js#L3969) are integer IDs. |
| attachments.ts:33 | `Zotero.Fulltext.getItemContent(attachmentItem.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/fulltext.js | ✗signature | **`Zotero.Fulltext.getItemContent` does not exist in Zotero 8 main.** Grepping fulltext.js for `getItemContent` returns zero matches. The public surface is: `getIndexedState(item)` (fulltext.js#L1327), `indexItems(itemIDs)` (fulltext.js#L460), `getItemCacheFile(item)` (fulltext.js#L1420), `setItemContent(libraryID, key, data, version)` (fulltext.js#L817), `getUnsyncedContent(libraryID, options)` (fulltext.js#L670), plus the content-shape object `{content, indexedChars, totalChars, indexedPages, totalPages}` assembled inside `getUnsyncedContent` (fulltext.js#L746-754). Replacement: for indexed items, read the cache file via `Zotero.File.getContentsAsync(Zotero.Fulltext.getItemCacheFile(item).path)` (fulltext.js#L692-696) and fetch the chars/pages row from the `fulltextItems` table directly (fulltext.js#L672 SQL) or call `Zotero.PDFWorker.getFullText(itemID, maxPages)` (pdfWorker/manager.js#L644) for on-demand extraction. The `(Zotero.Fulltext as any).getItemContent` cast — plus the `content?.content OR_ELSE ""` default (OR_ELSE stands in for the JS `&#124;&#124;` operator — markdown tables mangle the literal pipe) — silently masks the missing-method error at runtime, so `attachments.getFulltext` currently returns `{content: "", indexedChars: 0, totalChars: 0}` for every call. |
| attachments.ts:43 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. |
| attachments.ts:48 | `Zotero.Items.getAsync(attIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. |
| attachments.ts:53 | `Zotero.PDFWorker.getOutline(attachmentItem.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/pdfWorker/manager.js | ✗signature | **`Zotero.PDFWorker.getOutline` does not exist in Zotero 8 main.** The complete public surface of the `PDFWorker` class in pdfWorker/manager.js is: `canImport()` (manager.js#L187), `export(itemID, path, isPriority, password, transfer)` (manager.js#L193), `exportParent(item, directory, isPriority)` (manager.js#L270), `import(itemID, isPriority, password, transfer)` (manager.js#L298), `processCitaviAnnotations` (manager.js#L398), `processMendeleyAnnotations` (manager.js#L432), `importParent(item, isPriority)` (manager.js#L463), `deletePages(itemID, pageIndexes, ...)` (manager.js#L487), `rotatePages` (manager.js#L592), `getFullText(itemID, maxPages, isPriority, password)` (manager.js#L644), `getRecognizerData(itemID, ...)` (manager.js#L690), `renderAttachmentAnnotations(itemID, ...)` (manager.js#L726), `hasAnnotations(itemID, ...)` (manager.js#L790). No `getOutline`. This handler throws a runtime `TypeError: Zotero.PDFWorker.getOutline is not a function`. There is no official replacement in main — outline extraction is performed inside the reader frontend via pdfjs directly, not via `PDFWorker`. Fix options: (a) remove the method entirely; (b) extract via `pdfjs-dist` loaded in-worker. Confirm against a Zotero 8.0.4 runtime before the rewrite. |
| attachments.ts:58 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. |
| attachments.ts:60 | `Zotero.File.pathToFile(params.path)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/file.js#L43 | ⚠cumbersome | `pathToFile` converts `string OR_ELSE nsIFile` → `nsIFile` (file.js#L43-57 accepts already-nsIFile and returns it unchanged). Functionally correct. However `Zotero.Attachments.importFromFile` (attachments.js#L62) ALSO calls `pathToFile` internally on `options.file`, so this handler double-wraps. Minor. Could pass `params.path` (a string) directly as `options.file` and skip the handler-side nsIFile materialization — but then the `file.exists()` guard on line 61 would need to become `await OS.File.exists(params.path)` or similar. Keep the current pattern for the explicit-guard readability; it's not broken. |
| attachments.ts:62 | `Zotero.Attachments.importFromFile({file, parentItemID, title})` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/attachments.js#L58 | ✓correct | Destructured params exactly match attachments.js#L61-71: `libraryID`, `file`, `parentItemID`, `title`, `collections`, `fileBaseName`, `contentType`, `charset`, `saveOptions`. Handler passes `file` + `parentItemID` + `title` — all valid. libraryID is inferred from parentItemID at attachments.js#L94-97 (good, so omitting it is correct). Returns the newly-saved `Zotero.Item` (attachments.js#L56 JSDoc, #L112 `.save`). |
| attachments.ts:71 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. |
| attachments.ts:73 | `Zotero.Attachments.importFromURL({url, parentItemID, title})` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/attachments.js#L523 | ✓correct | Destructured params match attachments.js#L523-537: `libraryID`, `url`, `parentItemID`, `collections`, `title`, `fileBaseName`, `renameIfAllowedType`, `contentType`, `referrer`, `saveOptions`. Handler passes `url` + `parentItemID` + `title` — all valid. Returns the new Item. |
| attachments.ts:82 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. Handler uses `item.getFilePathAsync()` (item.js#L2734) which is the recommended async variant. |
| attachments.ts:93 | `Zotero.Attachments.addAvailablePDF(parent)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/attachments.js#L1825 | ⚠deprecated | The method still exists as a back-compat shim at attachments.js#L1825-1828 but emits `Zotero.warn('Zotero.Attachments.addAvailablePDF() is deprecated -- use addAvailableFile()')` on every call. The replacement is `Zotero.Attachments.addAvailableFile(item, options = {})` (attachments.js#L1816). Signature is identical (`item`, optional `options.methods`). Switch the call to silence the deprecation log and future-proof against removal. Return shape is the same: `Zotero.Item` OR_ELSE `false` (OR_ELSE stands in for the JS union-pipe — markdown tables mangle the literal pipe). |

## notes.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| notes.ts:6 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. |
| notes.ts:9 | `Zotero.Items.getAsync(noteIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. `parent.getNotes()` (item.js#L2409) returns integer IDs of child note items (IDs-only, see item.js#L2430-2437). |
| notes.ts:21 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. |
| notes.ts:23 | `new Zotero.Item("note")` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L30 | ✓correct | Constructor accepts itemType string and calls `setType(Zotero.ItemTypes.getID("note"))` internally (item.js#L91-94). `"note"` is a valid built-in itemType. Subsequent `note.libraryID = ...`, `note.parentID = ...`, `note.setNote(content)` (item.js#L2364), `note.addTag(tag)` (item.js#L4518), `note.saveTx()` (dataObject.js#L1012) all use documented setters. Standard pattern. |
| notes.ts:35 | `Zotero.Items.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + `isNote()` guard (item.js#L2257). |
| notes.ts:44 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Read-only getter defined via `Zotero.defineProperty` (libraries.js#L28-40). |
| notes.ts:45 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Constructor accepts optional `params = {}` object (search.js#L27). Empty-ctor then property-assignment is the documented pattern. `libraryID` setter at search.js#L108 delegates to `_set('libraryID', val)`. |
| notes.ts:48 | `s.addCondition("note", "contains", params.query)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L297 | ✓correct | `addCondition(condition, operator, value, required)` (search.js#L297). `"note"` is a registered condition handler (search.js references it alongside `fulltextContent`, `tag`, `title`, etc.). `"contains"` is a standard string operator. |
| notes.ts:50 | `Zotero.Items.getAsync(ids.slice(0, limit))` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. `ids` from `s.search()` (search.js#L541) is `Promise<number[]>` — integer IDs. |
| notes.ts:60 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. `getAnnotations` handler entry point. |
| notes.ts:66 | `Zotero.Items.getAsync(attIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. |
| notes.ts:71 | `pdfItem.getAnnotations()` + `as unknown as number[]` cast | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L4380 | ✗signature | **The TS cast `as unknown as number[]` is a lie.** `getAnnotations(includeTrashed, asIDs)` (item.js#L4380) returns `Zotero.Item[]` when `asIDs` is falsy — see item.js#L4394-4395 / #L4405-4406: `if (asIDs) return ids; return Zotero.Items.get(ids)`. Handler calls with no args, so `asIDs=undefined` → returns already-hydrated Item instances. The subsequent `Zotero.Items.getAsync(annotationIDs)` on line 72 then runs `Number.isInteger(id)` on each Item (dataObjects.js#L175) — the integer check fails and triggers the `parseInt(instance)` → `NaN` path (dataObjects.js#L179), so `_objectCache[NaN]` never hits and every annotation is dropped silently in production. Additionally, `getAnnotations()` throws `"getAnnotations() can only be called on file attachments"` (item.js#L4381-4383) if `pdfItem` isn't a file attachment — handler's `isAttachment()` guard is satisfied because we narrow to the PDF on lines 63-70, but a non-file attachment (web snapshot) would still trip it. Fix: either pass `getAnnotations(false, true)` to get IDs and keep the current `getAsync`, or drop the cast + extra `getAsync` call and use the returned items directly. |
| notes.ts:72 | `Zotero.Items.getAsync(annotationIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✗signature | Downstream of notes.ts:71. Given that `annotationIDs` is actually `Zotero.Item[]` not `number[]`, this call mis-types its input. See notes.ts:71 row for the repro. The fix is at the source — change the `getAnnotations()` call — after which this row becomes ✓correct. |
| notes.ts:94 | `Zotero.Items.getAsync(params.parentId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Scalar form + null-guard. `createAnnotation` entry point. |
| notes.ts:99 | `Zotero.Items.getAsync(attIDs)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. |
| notes.ts:104 | `new Zotero.Item("annotation")` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/item.js#L30 | ⚠cumbersome | Constructor accepts `"annotation"` (item.js#L91-94; `annotation` is a registered itemType). Subsequent setters `(annotation as any).annotationType = params.type` (item.js#L4201-L4220 loop-defined setter), `annotation.annotationText` (same loop), `annotation.annotationComment`, `annotation.annotationColor`, `annotation.annotationPosition` (item.js#L4301-4321 deferred setter, requires string — handler correctly `JSON.stringify`s), `annotation.saveTx()` (dataObject.js#L1012) — all documented. However item.js#L4223-4224 enforces: *"annotationType must be set before other annotation properties"*; handler on lines 107-111 sets `annotationType` FIRST, then text/comment/color/position — order is correct. Additionally item.js#L4243-4244: `annotationText` is only valid for `highlight` / `underline` types — handler does `if (params.text) annotation.annotationText = params.text` which will throw for image/ink/note annotations even when the caller supplies text. Cumbersome: caller has no way to know text is ignored unless it happens to be a highlight/underline. Better: gate the `annotationText` assignment on `params.type` being `highlight` or `underline`, or throw `-32602` up-front with a clearer message. Annotation color also must be 6-char hex (item.js#L4249-4254) — handler default `"#ffd400"` is valid, but user-supplied `params.color` is not validated before the setter throws. Verdict: ⚠cumbersome — the API works but the handler has no parameter-validation layer for invariants the setter enforces, so callers get raw Zotero errors instead of `-32602`. |

## search.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| search.ts:8 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Read-only getter. |
| search.ts:9 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. |
| search.ts:11 | `s.addCondition("quicksearch-titleCreatorYear", "contains", params.query)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L297 | ✓correct | `quicksearch-titleCreatorYear` is one of Zotero's three quicksearch virtual conditions (alongside `-titleCreatorYearNote` and `-everything`). It expands into OR-joined title/creator/year subconditions internally. `"contains"` is the required operator. |
| search.ts:12 | `s.addCondition("noChildren", "true", "")` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L297 | ⚠cumbersome | `noChildren` handling at search.js#L1030-1031 checks `condition.operator == 'true'` — value field is ignored entirely. Zotero's own internal call-sites use the 2-argument form `s.addCondition('noChildren', 'true')` (search.js#L356). The empty-string third arg is harmless but non-canonical. Minor style issue, not a bug. Apply `replace_all` across all 5 search.ts occurrences (L12 / L29 / L44 / L60 / L77). |
| search.ts:13 | `s.search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L541 | ✓correct | Returns `Promise<number[]>` — item IDs (search.js#L541 JSDoc and implementation). Handler correctly uses IDs. |
| search.ts:14 | `Zotero.Items.getAsync(ids.slice(0, limit))` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. |
| search.ts:23 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:24 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. |
| search.ts:31 | `Zotero.Items.getAsync(ids)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ⚠cumbersome | `search.advanced` has no `limit` parameter, so the handler fetches every matched item in one shot. For a broad advanced query (thousands of items) this can saturate the RPC response. No bug — but consistency with `quick` / `fulltext` / `byTag` / `byIdentifier` would argue for an optional `limit` with a default. Flag for PRD (internal consistency). |
| search.ts:40 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:41 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. |
| search.ts:46 | `Zotero.Items.getAsync(ids.slice(0, limit))` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. `fulltextContent` + `"contains"` is a valid Zotero condition (search.js references it alongside `fulltextWord`). |
| search.ts:56 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:57 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. |
| search.ts:62 | `Zotero.Items.getAsync(ids.slice(0, limit))` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. |
| search.ts:71 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:72 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. `byIdentifier` then conditionally adds DOI/ISBN/ISSN conditions. The `params.pmid` arm in the TS signature (line 70) is dead code — CLI removed `--pmid` in commit b7b5e21, so no caller can populate it. Already documented in Task 2's audit; no row here. |
| search.ts:79 | `Zotero.Items.getAsync(ids)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Array form. `byIdentifier` also has no `limit` — same observation as `advanced` (search.ts:31). |
| search.ts:84 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:85 | `Zotero.Searches.getByLibrary(libraryID)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/searches.js#L52 | ⚠cumbersome | `Zotero.Searches.getByLibrary` (searches.js#L52-67) iterates `this._objectCache` ONLY — it does not hit the DB. Freshly-opened Zotero instances that haven't had `Zotero.Searches.getAll(libraryID)` (searches.js#L75-89) or an individual `getAsync` call yet will see an empty object cache and `getByLibrary` returns `[]`. This is effectively a cold-cache bug: `savedSearches` RPC returns no results on the first call after XPI reload until the user opens a saved-search somewhere else in the UI. Fix: use `Zotero.Searches.getAll(libraryID)` (searches.js#L75) which runs `SELECT savedSearchID FROM savedSearches WHERE libraryID=?` against the DB and then loads each. `getAll` is async and already returns searches sorted by name — drop-in replacement minus the `await`. Also the `(Zotero.Searches as any).getByLibrary` cast exists only because the TS bindings don't type it; underlying method is public. |
| search.ts:98 | `new Zotero.Search()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/search.js#L27 | ✓correct | Empty ctor. Followed by `s.name = params.name` (search.js#L61 setter), `s.addCondition` loop (search.js#L297), `s.saveTx()` (dataObject.js#L1012 via Search._saveData at search.js#L170). Standard create pattern. |
| search.ts:99 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter. |
| search.ts:109 | `Zotero.Searches.getAsync(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | `Zotero.Searches` inherits `getAsync` from `Zotero.DataObjects.prototype.getAsync` (searches.js#L204 `Zotero.DataObjects.call(this)` chain; dataObjects.js#L152). Scalar form returns a single `Zotero.Search` or `false`; null-guard matches. `s.eraseTx()` on line 111 is `Zotero.DataObject.prototype.eraseTx` (dataObject.js#L1316). |

---


Target: three handlers, Zotero 8.0.4. Row format / verdicts per design doc
(`2026-04-23-xpi-api-audit-design.md`).

**Pipe-safety legend** (applies to every row's `note` column):

- `OR_ELSE` stands in for the JS logical-OR operator (`||`), since a raw
  `|` would split the markdown table.
- `&#124;` is the HTML-entity form used when embedding a code snippet
  inline that needs to carry a literal pipe (e.g. a TS union type).

Scope note: this table may include calls on Zotero-returned instances
(`lib.editable`, `pane.getSelectedCollection`, `item.save`) that the narrow
`Zotero\.\w+\.\w+|new Zotero\.\w+` regex doesn't pick up. Such calls get
their own rows keyed on real line numbers when they carry findings or verify
load-bearing signatures.

**Actual enumeration totals declared here after Step 3:**

- `system.ts`: narrow-grep finds **15** `Zotero.*` call sites. The plan said
  "expected 17"; the 2-count gap is because the narrow regex `Zotero\.\w+\.\w+`
  requires two dots after `Zotero`, which excludes `Zotero.version` (line 5)
  and `Zotero.getActiveZoteroPane()` (line 41). Both are audited as rows.
  Two further splits from instance access: `lib.id` / `lib.libraryType` /
  `lib.name` / `lib.editable` share one row on line 8 (the narrow regex
  misses property access on a returned Library instance, and the
  `id`/`libraryID` alias is load-bearing per `library.js#L121-124`), and
  `pane.getSelectedCollection()` gets its own row on line 43 (UI-context-
  dependent, tied to the `getActiveZoteroPane` finding). Table has **19
  rows** — 15 narrow + 2 regex-missed (`version`, `getActiveZoteroPane`) +
  2 instance splits (line 8 Library props, line 43 ZoteroPane method).
- `settings.ts`: narrow-grep finds **5** `Zotero.*` call sites (matches
  plan). No instance-method splits; all 5 are audited in place. Table has
  **5 rows**.
- `tags.ts`: narrow-grep finds **10** `Zotero.*` call sites. The plan said
  "expected 11"; the gap is the narrow regex missing nothing semantically —
  all 10 are present. One extra split row: `item.save()` inside
  `Zotero.DB.executeTransaction` at tags.ts:64 (load-bearing — verifies the
  `save()` vs `saveTx()` contract per `dataObject.js#L913-924` (save emits
  the transaction-warning) and `dataObject.js#L1012` (saveTx), which is
  the exact idiom the bibliography-style mismatch would break). Table has
  **11 rows**.

Total rows across three sections: **35** (19 + 5 + 11). Narrow-grep total is
**30** (15 + 5 + 10). The +5 widening: 2 regex-missed in system.ts
(`version`, `getActiveZoteroPane`), 1 instance-property row in system.ts
(line 8 Library props), 1 instance-method row in system.ts (line 43
`pane.getSelectedCollection`), 1 instance-method row in tags.ts (line 64
`item.save` inside transaction).

## system.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| system.ts:5 | `Zotero.version` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/zotero.js#L233 | ✓correct | Property set from `Services.appinfo.version` at zotero.js#L233 — stable global read, no signature to break. Narrow regex misses it (only one dot after `Zotero`). |
| system.ts:7 | `Zotero.Libraries.getAll()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L154 | ✓correct | `this.getAll = function ()` (libraries.js#L154-165). Returns `Zotero.Library[]` sorted user-first then alphabetical. No args; handler calls it correctly. |
| system.ts:8 | `lib.id` / `lib.libraryType` / `lib.name` / `lib.editable` (instance props on Library) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/library.js#L116 | ✓correct | Library instance exposes: `libraryID` getter (library.js#L116-119), `id` alias via `Zotero.defineProperty` (library.js#L121-124 — `get: function () { return this.libraryID; }`), `libraryType` (library.js#L126-129), `name` (library.js#L179-188 — computed/localized for user lib), `editable` (library.js#L212-220 — defined via an accessor loop over `['editable', 'filesEditable', 'storageVersion', 'archived', 'isAdmin']`). All four are public prototype properties. The `.id`-over-`.libraryID` choice is a defensible JSON-boundary convention (PRD's integer-ID rule) since Zotero itself defines `.id` as an alias. |
| system.ts:11 | `Zotero.Libraries.get(params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L174 | ✓correct | `this.get = function (libraryID)` (libraries.js#L174-176). Returns `Zotero.Library` instance or `false` via `this._cache[libraryID] OR_ELSE false` — handler's `if (!lib)` guard is correct for the falsy-not-found contract. |
| system.ts:13 | `Zotero.Prefs.set("lastLibraryID", params.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L275 | ✗signature | `function set(pref, value, global)` at prefs.js#L275; `global` defaults to `false`, and when false the function prepends `ZOTERO_CONFIG.PREF_BRANCH` (= `"extensions.zotero."`) to the key (prefs.js#L277 — `pref = global ? pref : ZOTERO_CONFIG.PREF_BRANCH + pref`). This call therefore writes to `extensions.zotero.lastLibraryID` — **the handler is polluting Zotero's own pref branch**, not the plugin's branch. Correct form (matching settings.ts:38's convention): `Zotero.Prefs.set("extensions.zotero-bridge.lastLibraryID", params.id, true)` — pass the full absolute path with `global=true`. Note: `params.id` is a number, prefs accept number/string/bool at prefs.js#L281-302 (switch on `branch.getPrefType`), so the value type is fine; only the key namespace is wrong. |
| system.ts:17 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Getter on `Zotero.Libraries` at libraries.js#L28-35 (defined via `Zotero.defineProperty(this, 'userLibraryID', { get: ... })`). Throws "Library data not yet loaded" if accessed before init — at RPC-call time libraries are always initialized, so this is safe. Integer return; matches PRD integer-ID rule. |
| system.ts:18 | `Zotero.Items.getAll(libraryID, false, false)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/items.js#L121 | ⚠cumbersome | `this.getAll = async function (libraryID, onlyTopLevel, includeDeleted, asIDs=false)` at items.js#L121-140. Positional args are correct. However for a **stats-only** count (handler only reads `items.length`), hydrating full `Zotero.Item` objects is wasteful on libraries of 10k+ items — with `asIDs` omitted the function ends with `return this.getAsync(ids)` (items.js#L139). Replace with `Zotero.Items.getAll(libraryID, false, false, true)` (fourth arg `asIDs=true`) to short-circuit to the raw integer-ID list (items.js#L136-138 `if (asIDs) return ids;`) — same `.length`, no object materialization. Non-blocking, but documented for the fix pass. |
| system.ts:19 | `Zotero.Collections.getByLibrary(libraryID, false)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/collections.js#L71 | ⚠cumbersome | `this.getByLibrary = function (libraryID, recursive, includeTrashed)` at collections.js#L71-73. Passing `recursive=false` returns only top-level collections; for a "library stats" display users expect **all** collections including nested. Replace with `Zotero.Collections.getByLibrary(libraryID, true)` (or document that `.collections` means "top-level collections only"). If the PRD picks "all", this is a shape divergence against `items` (which counts everything). Minor — confirm semantic intent in PRD. |
| system.ts:23 | `Zotero.ItemTypes.getAll()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L119 | ✓correct | `this.getAll = this.getTypes = function ()` in the cached-types base class (cachedTypes.js#L119-125 — inherited by both `Zotero.ItemTypes` at cachedTypes.js#L341 and `Zotero.CreatorTypes` at cachedTypes.js#L218). Returns `this._typesArray` populated as `[{id, name}, ...]` — handler's `t.name` + `t.id` destructuring is correct. |
| system.ts:24 | `Zotero.ItemTypes.getLocalizedString(t.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L421 | ✓correct | `this.getLocalizedString = function (idOrName)` at cachedTypes.js#L421 (inside the `Zotero.ItemTypes` block at cachedTypes.js#L341+) accepts both int ID and string name. Handler passes int ID — correct. Returns localized label from `Zotero.Schema.globalSchemaLocale.itemTypes`, with custom-type and camelCase fallbacks. |
| system.ts:27 | `Zotero.ItemTypes.getID(params.itemType)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L98 | ✓correct | `this.getID = function (idOrName)` in the cached-types base class at cachedTypes.js#L98-116. Returns integer ID OR_ELSE `false` on unknown name (cachedTypes.js#L113 `return false;`). Handler's `if (!typeID)` guard matches the falsy-not-found contract. |
| system.ts:29 | `Zotero.ItemFields.getItemTypeFields(typeID)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/itemFields.js#L213 | ✓correct | `function getItemTypeFields(itemTypeID)` at itemFields.js#L213-229 (exposed on `this` at itemFields.js#L48). Returns `[..._itemTypeFields[itemTypeID]]` — a copy-array of numeric field IDs. Handler correctly types `fields.map((fid: number) => ...)` — no object wrapping needed. |
| system.ts:30 | `Zotero.ItemFields.getName(fid)` / `.getLocalizedString(fid)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/itemFields.js#L129 | ✓correct | `function getName(field)` at itemFields.js#L129-135 (exposed on `this` at itemFields.js#L44) returns the English programmatic name (e.g. `"title"`, `"publicationTitle"`) via `_fields[field]['name']`, OR_ELSE `false` when unknown. `this.getLocalizedString = function (field)` at itemFields.js#L143-167 returns the user-facing label; both accept ID or name. Handler passes numeric ID from `getItemTypeFields` — correct. |
| system.ts:33 | `Zotero.ItemTypes.getID(params.itemType)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L98 | ✓correct | Duplicate of line 27 — same contract, same guard, correct. |
| system.ts:35 | `Zotero.CreatorTypes.getTypesForItemType(typeID)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L280 | ✓correct | `this.getTypesForItemType = function (itemTypeID)` at cachedTypes.js#L280-285 (inside `Zotero.CreatorTypes` at cachedTypes.js#L218). Returns `_creatorTypesByItemType[itemTypeID]` (populated as `[{id, name}, ...]`), OR_ELSE an empty array. Handler's `t.name` + `t.id` destructuring is correct. The `getID` guard at line 33 prevents the unknown-itemType-id empty-array case. |
| system.ts:36 | `Zotero.CreatorTypes.getLocalizedString(t.id)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/cachedTypes.js#L310 | ✓correct | `this.getLocalizedString = function (idOrName)` at cachedTypes.js#L310-313 (inside the `Zotero.CreatorTypes` block). Resolves `this.getName(idOrName)` then returns `Zotero.Schema.globalSchemaLocale.creatorTypes[name]`. Handler passes int ID. Correct. |
| system.ts:38 | `Zotero.Sync.Runner.sync({ libraries: "all" })` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/sync/syncRunner.js#L116 | ✗signature | `this.sync = Zotero.serial(function (options = {}) { return this._sync(options); })` at syncRunner.js#L116-118. `options.libraries` is **documented as an integer array of library IDs** (syncRunner.js#L105-114 JSDoc `@param {Integer[]} [options.libraries] IDs of libraries to sync`). At syncRunner.js#L229-233 the value is fed through `options.libraries ? Array.from(options.libraries) : []` — `Array.from("all")` yields `["a","l","l"]` (three char elements), which then pass into `checkLibraries` as bogus library IDs. The `"all"` string is **not a documented sentinel**; the "sync every accessible library" behavior is triggered by **omitting** the key — at syncRunner.js#L418 `checkLibraries` computes `var syncAllLibraries = !libraries OR_ELSE !libraries.length;`. Correct form: `await Zotero.Sync.Runner.sync({})` — or, if the RPC needs to be explicit, `await Zotero.Sync.Runner.sync()`. Pass integer IDs only when scoping to specific libraries. |
| system.ts:41 | `Zotero.getActiveZoteroPane()` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/zotero.js#L93 | ⚠cumbersome | `this.getActiveZoteroPane = function () { var win = Services.wm.getMostRecentWindow("navigator:browser"); return win ? win.ZoteroPane : null; };` at zotero.js#L93-96. **UI-dependent:** returns `null` whenever no navigator:browser window is active (headless startup, minimized-to-tray, Zotero started but no window yet shown, backend RPC fired before window init). The handler's `if (!pane) return null;` guard correctly handles the null case, so the call is not unsafe — but `system.currentCollection` silently becomes `null` for any caller hitting the XPI before the user opens the main window. For a bootstrap-plugin backend this is "works on developer's machine, sometimes null in production". Alternatives: (a) iterate `Zotero.getZoteroPanes()` at zotero.js#L98-108 (enumerates every `navigator:browser` window that has a `.ZoteroPane`) and pick the first; (b) explicitly document that `system.currentCollection` requires the main window to be foregrounded. Narrow regex misses this call (only one dot). |
| system.ts:43 | `pane.getSelectedCollection()` (instance method on ZoteroPane) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/zoteroPane.js | ?unverifiable | `ZoteroPane.getSelectedCollection` is a frontend (chrome/content/zotero/zoteroPane.js) method, not an xpcom backend module. Signature/return type is documented only by usage convention: returns a `Zotero.Collection` instance, or `null`/`false` if the selected row is a library/tag/saved-search rather than a collection. Handler's `if (!col) return null;` guard covers both falsy paths. Cannot verify against a specific `#L<n>` anchor from the xpcom layer; cite bare-file URL. Pattern is consistent with how Zotero's own reader/quickCopy code reads the pane (many call sites in `chrome/content/zotero/*.js`). Mark unverifiable-from-docs, not blocking. |

## settings.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| settings.ts:22 | `Zotero.Prefs.get(PREF_PREFIX + params.key, true)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L233 | ✓correct | `function get(pref, global)` at prefs.js#L233. `global=true` bypasses the `extensions.zotero.` prefix (prefs.js#L235 `pref = global ? pref : ZOTERO_CONFIG.PREF_BRANCH + pref;`), so the full absolute path `extensions.zotero-bridge.<key>` is used — **this is the correct pattern** for plugin-owned prefs. Return is `undefined` for missing keys (handler applies `?? null` explicitly, matching PRD null-for-absent rule). |
| settings.ts:28 | `Zotero.Prefs.get(PREF_PREFIX + key, true) ?? null` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L233 | ✓correct | Same contract as line 22, iterating over `SETTINGS_KEYS`. Correct. |
| settings.ts:38 | `Zotero.Prefs.set(PREF_PREFIX + params.key, params.value, true)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L275 | ✓correct | `function set(pref, value, global)` at prefs.js#L275. `global=true` + full path = plugin-owned branch (prefs.js#L277 prepend guard). Accepts `string/number/bool` via the `switch (branch.getPrefType(pref))` block at prefs.js#L281-302, and creates new prefs in the appropriate type automatically for the unknown case (prefs.js#L289-302) — handler passes `params.value: any`, which is fine for valid Firefox pref types; invalid types (object/array) will throw from nsIPrefBranch at prefs.js#L302 `throw new Error("Invalid preference value ...")`, not silently corrupt. Matches the convention system.ts:13 violates. |
| settings.ts:45 | `Zotero.Prefs.get(PREF_PREFIX + key, true) ?? null` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L233 | ✓correct | Duplicate of line 28 (dead-code wrapper — `getAll()` duplicates the fall-through of `get()` with no `params.key`; consolidation is an internal-audit concern, not external-API). |
| settings.ts:54 | `Zotero.Prefs.set(PREF_PREFIX + key, value, true)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/prefs.js#L275 | ✓correct | Batch set, same contract as line 38. Correct. Note: whitelist filter on line 53 (`SETTINGS_KEYS.includes(key)`) silently drops unknown keys — single-key `set()` throws on unknown, `setAll()` does not. Shape divergence flagged here; internal-audit will catalog. |

## tags.ts

| handler:line | zotero-call | doc-ref | verdict | note |
|---|---|---|---|---|
| tags.ts:7 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Integer getter at libraries.js#L28-35. Safe at RPC-call time. Note: `tags.list` / `tags.rename` / `tags.delete` all hard-default to the user library only — no `libraryID` param is accepted. For group-library tags these methods silently miss. Shape divergence (PRD should enforce `libraryID?` param family-wide); internal-audit will catalog. |
| tags.ts:8 | `Zotero.Tags.getAll(libraryID)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/tags.js#L145 | ✓correct | `this.getAll = async function (libraryID, types)` at tags.js#L145-147 (delegates to `this.getAllWithin({ libraryID, types })` at tags.js#L160). Returns `[{tag: "foo"}, {tag: "bar", type: 1}]` — API-JSON shape where `type` is absent for manual tags (type 0). Handler maps `{tag: t.tag, type: t.type}` — for manual tags `t.type` is `undefined`, which JSON.stringify drops. Correct but inconsistent with PRD null-for-absent rule; consider `type: t.type ?? 0` to explicit-zero manual tags. Minor. |
| tags.ts:16 | `Zotero.Items.getAsync(params.itemId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | `Zotero.DataObjects.prototype.getAsync = async function (ids, options)` at dataObjects.js#L152. Scalar form is wrapped into a single-element array at dataObjects.js#L164-170 and returned via `return toReturn.length ? toReturn[0] : false;` at dataObjects.js#L207. Handler's `if (!item)` guard matches the falsy-not-found contract. Correct. |
| tags.ts:26 | `Zotero.Items.getAsync(params.itemId)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObjects.js#L152 | ✓correct | Duplicate of line 16 in `tags.remove`. Correct. |
| tags.ts:36 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Same as line 7 — user-lib-only limitation noted once, not re-flagged here. |
| tags.ts:37 | `Zotero.Tags.rename(libraryID, oldName, newName)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/tags.js#L279 | ✓correct | `this.rename = async function (libraryID, oldName, newName)` at tags.js#L279-360. Positional args match. Handler returns `{renamed: true, from, to}` without awaiting the rename throwing — if the old tag doesn't exist, `Zotero.Tags.rename` is a no-op (early-returns after the `oldName == newName` / missing-tag guards at tags.js#L285-290), so `renamed: true` may be misleading. Internal-audit shape concern; external-API call itself is correct. |
| tags.ts:42 | `Zotero.Libraries.userLibraryID` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/libraries.js#L28 | ✓correct | Same as line 7. |
| tags.ts:43 | `Zotero.Tags.getID(params.tag)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/tags.js#L83 | ✓correct | `this.getID = function (name)` at tags.js#L83-96. Returns integer tagID OR_ELSE `false` via `return id !== undefined ? id : false;` (tags.js#L95). Handler's `if (!tagID)` guard matches falsy-not-found contract. Correct. Note: tags.js#L87-89 now throws if a second argument is passed (`"no longer takes a second parameter"`) — handler passes one arg, so safe. |
| tags.ts:45 | `(Zotero.Tags as any).removeFromLibrary(libraryID, tagID)` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/tags.js#L370 | ✓correct | `this.removeFromLibrary = async function (libraryID, tagIDs, onProgress, types)` at tags.js#L370-463. **Public method** (no underscore prefix). Handler passes `(libraryID, tagID)` — `tagIDs` accepts Integer OR_ELSE Integer-array, scalar form valid. The `as any` cast is a **typings gap** (`typings/` has no `Zotero.Tags` ambient type), not an API-shape problem — Zotero exposes the method publicly. Fix direction: add ambient types, drop the cast. Not a signature bug. |
| tags.ts:54 | `Zotero.DB.executeTransaction(async () => { ... })` | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/db.js#L432 | ✓correct | `Zotero.DBConnection.prototype.executeTransaction = async function (func, options = {})` at db.js#L432. `func` must be async — db.js#L472-475 throws `"Zotero.DB.executeTransaction() no longer takes a generator function -- pass an async function instead"` for `GeneratorFunction`. Serializes concurrent transactions via the `while (this._transactionID)` wait-loop at db.js#L439-447 (with `waitTimeout` default 30s). Handler's `async () => {...}` wrapper is the right shape. Correct. |
| tags.ts:64 | `item.save()` inside `executeTransaction` (instance method on Item) | https://github.com/zotero/zotero/blob/main/chrome/content/zotero/xpcom/data/dataObject.js#L913 | ✓correct | `Zotero.DataObject.prototype.save = async function (options = {})` at dataObject.js#L913 emits `Zotero.logError("save() called on Zotero.<type> without a wrapping transaction -- use saveTx() instead")` at dataObject.js#L919-924 whenever `!env.options.tx && !Zotero.DB.inTransaction()`. Here the call is inside `executeTransaction`, so `Zotero.DB.inTransaction()` is true and the warning does not fire — this is exactly the sanctioned pattern. `saveTx` is defined at dataObject.js#L1012. By contrast `tags.add` (line 21) and `tags.remove` (line 31) call `item.saveTx()` outside any transaction, which is also correct. `batchUpdate` is the only path where `save()` is appropriate; swapping it back to `saveTx()` would nest transactions and trip the wait-loop at db.js#L439-447. |

## End of table.

Verdict counts (from final table data — recounted after commit):

- system.ts (19 rows): ✓correct = 13, ✗signature = 2, ⚠cumbersome = 3, ⚠deprecated = 0, ?unverifiable = 1
- settings.ts (5 rows): ✓correct = 5
- tags.ts (11 rows): ✓correct = 11

Total: 35 rows, ✓=29, ✗=2, ⚠=3, ?=1.

✗signature findings (2):

- `system.ts:13` — `Zotero.Prefs.set("lastLibraryID", params.id)` writes to
  `extensions.zotero.lastLibraryID` (Zotero's own branch), not the plugin
  branch. Fix: full path + `global=true`.
- `system.ts:38` — `Zotero.Sync.Runner.sync({libraries: "all"})` feeds
  `Array.from("all")` → `["a","l","l"]` into `checkLibraries`. Fix: drop
  the key or pass an integer array.

?unverifiable findings (1):

- `system.ts:43` — `pane.getSelectedCollection()` is a frontend ZoteroPane
  method; no xpcom `#L<n>` anchor. Behavior inferred from Zotero's own
  usage, guarded by the handler.
